<%= form_with(model: project_user) do |form| %>
  <% if project_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_user.errors.count, "error") %> prohibited this project_user from being saved:</h2>

      <ul>
        <% project_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
